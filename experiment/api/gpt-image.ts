/**
 * GPT Image — SKIPPED
 *
 * OPENAI_API_KEY가 없어서 이번 실험에서 제외.
 * 향후 키를 확보하면 gpt-image-1 모델로 테스트 가능.
 *
 * 예정 파라미터:
 *   - model: "gpt-image-1"
 *   - size: "1024x1024"
 *   - quality: "high"
 *   - background: "transparent"
 *   - output_format: "png"
 */

export const GPT_IMAGE_SKIPPED = true;
export const SKIP_REASON = "OPENAI_API_KEY not available";
